using Xunit;

namespace Boxwright.Core.Tests;

// Placeholder so the BLD-1 build/test baseline is green. Replaced by real tests
// as Boxwright.Core gains behavior — VmConfig round-trip and exhaustive
// CommandLineBuilder coverage (see docs/backlog.md, CORE-1 onward).
public class PlaceholderTests
{
    [Fact]
    public void TestInfrastructure_IsWired() => Assert.True(true);
}
